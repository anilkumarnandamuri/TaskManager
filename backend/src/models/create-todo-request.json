{
    "$schema": "http://json-schema.org/draft-04/schema",
    "type": "object",
    "title": "TODO Schema",
    "properties": {
        "name": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        },
        "dueDate": {
            "type": "string",
            "pattern": "^(?!\\s*$).+"
        }
    },
    "required": [
        "name",
        "dueDate"
    ],
    "additionalProperties": false
}